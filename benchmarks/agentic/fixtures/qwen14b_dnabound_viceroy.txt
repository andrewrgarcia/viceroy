To fix the issue with `CPC` functions reading out of bounds, we need to adjust the guard conditions in the `if` statements. Specifically, for `CPC`, we need to ensure that the indices used do not exceed the array bounds.

Here is the corrected version of the `CPC` function:

```c
float CPC(int i1, int jc, float x1[chain_maxsize][max_size], float y1[chain_maxsize][max_size], float z1[chain_maxsize][max_size]) {
    float c1, c2, c3, p1, p2, p3, e1, f1, pp1, g1, g2, g3, gg1, hh1, di, ud0 = 0.0;
    
    // Check if the indices are within bounds
    if (i1 - 4 < 0 || i1 + 2 > L0_chain[jc]) {
        ud0 = 0.0;
    } else {
        c1 = ((y1[jc][i1 - 4] - y1[jc][i1 - 2]) * (z1[jc][i1 - 2] - z1[jc][i1 - 1]) -
               (z1[jc][i1 - 4] - z1[jc][i1 - 2]) * (y1[jc][i1 - 2] - y1[jc][i1 - 1]));
        c2 = ((z1[jc][i1 - 4] - z1[jc][i1 - 2]) * (x1[jc][i1 - 2] - x1[jc][i1 - 1]) -
               (x1[jc][i1 - 4] - x1[jc][i1 - 2]) * (z1[jc][i1 - 2] - z1[jc][i1 - 1]));
        c3 = ((x1[jc][i1 - 4] - x1[jc][i1 - 2]) * (y1[jc][i1 - 2] - y1[jc][i1 - 1]) -
               (y1[jc][i1 - 4] - y1[jc][i1 - 2]) * (x1[jc][i1 - 2] - x1[jc][i1 - 1]));
        
        p1 = ((y1[jc][i1 - 2] - y1[jc][i1 - 1]) * (z1[jc][i1 - 1] - z1[jc][i1]) -
               (z1[jc][i1 - 2] - z1[jc][i1 - 1]) * (y1[jc][i1 - 1] - y1[jc][i1]));
        p2 = ((z1[jc][i1 - 2] - z1[jc][i1 - 1]) * (x1[jc][i1 - 1] - x1[jc][i1]) -
               (x1[jc][i1 - 2] - x1[jc][i1 - 1]) * (z1[jc][i1 - 1] - z1[jc][i1]));
        p3 = ((x1[jc][i1 - 2] - x1[jc][i1 - 1]) * (y1[jc][i1 - 1] - y1[jc][i1]) -
               (y1[jc][i1 - 2] - y1[jc][i1 - 1]) * (x1[jc][i1 - 1] - x1[jc][i1]));
        
        e1 = sqrt(c1 * c1 + c2 * c2 + c3 * c3);
        f1 = sqrt(p1 * p1 + p2 * p2 + p3 * p3);
        pp1 = (c1 * p1 + c2 * p2 + c3 * p3) / (e1 * f1);
        
        g1 = (x1[jc][i1 - 4] - x1[jc][i1]);
        g2 = (y1[jc][i1 - 4] - y1[jc][i1]);
        g3 = (z1[jc][i1 - 4] - z1[jc][i1]);
        
        gg1 = sqrt(g1 * g1 + g2 * g2 + g3 * g3);
        hh1 = (p1 * g1 + p2 * g2 + p3 * g3) / (f1 * gg1);
        
        if (pp1 <= -1.0) {
            di = -3.14;
        } else if (pp1 >= 1.0) {
            di = 0.0;
        } else if (hh1 >= 0.0) {
            di = acos(pp1);
        } else {
            di = -acos(pp1);
        }
        
        ud0 = kcpcp * ((1 - cos(di - dcpcp)) + 0.5 * (1 - cos(3.0 * (di - dcpcp))));
    }
    
    return ud0;
}
```

In this corrected version, the guard condition checks if `i1 - 4` is less than 0 or if `i1 + 2` exceeds `L0_chain[jc]`. If either condition is true, `ud0` is set to 0.0, ensuring that the function does not attempt to access out-of-bounds indices.

You can apply this same logic to the other functions (`PCN`, `NCPC`) by adjusting the guard conditions accordingly:

```c
float PCN(int i1, int jc, float x1[chain_maxsize][max_size], float y1[chain_maxsize][max_size], float z1[chain_maxsize][max_size]) {
    // Similar logic as CPC but with different indices
}

float NCPC(int i1, int jc, float x1[chain_maxsize][max_size], float y1[chain_maxsize][max_size], float z1[chain_maxsize][max_size]) {
    // Similar logic as CPC but with different indices
}
```

Make sure to update the guard conditions in each function to ensure that they correctly handle out-of-bounds access.